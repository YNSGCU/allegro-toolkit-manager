# Instruction Entry Template

This runtime entry file should stay short.

- Point to `AGENTS.md` as the canonical shared instruction source.
- Point to `.structure-os/PROJECT_BRIEF.md`, `.structure-os/CURRENT_STATE.md`, and `.structure-os/HANDOFF.md`.
- Point to `.agents/protocol.md` and `.agents/ownership.json`.
- Add runtime-specific caveats only when they are not already covered elsewhere.
