# Allegro env - alias duplicates
# Testing: alias 完全相同别名重复检测

funckey F1 add connect
funckey F2 done

# 重复定义 alias zs
alias zs zoom selection
alias zs zoom fit           ; 和第一行 zs 冲突

# 重复定义 alias s
alias s save
alias s save_as             ; 和上一行 s 冲突

# 前缀关系（应仅低风险提示）
alias ss save_all
alias sss save_all_selected

funckey F8 autoFanout
