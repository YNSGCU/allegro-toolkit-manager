# Allegro env - funckey duplicates
# Testing: funckey 完全相同按键重复检测

funckey F1 add connect
funckey F2 done
funckey F3 cancel

# 重复定义 F8
funckey F8 autoFanout
funckey F8 showNetLength    ; 和上一行 F8 冲突

# 重复定义 F2
funckey F2 delete           ; 和第一行 F2 冲突

alias zs zoom selection
alias s save
